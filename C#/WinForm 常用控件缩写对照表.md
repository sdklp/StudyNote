1. 常见基础控件 (Common Controls)

| 控件类型 (Type) | 推荐前缀 (Prefix) | 命名示例 (Example)          |
| --------------- | ----------------- | --------------------------- |
| **Button**      | `btn`             | `btnSubmit`, `btnCancel`    |
| **TextBox**     | `txt`             | `txtPassword`, `txtSearch`  |
| **Label**       | `lbl`             | `lblTitle`, `lblStatus`     |
| **CheckBox**    | `chk`             | `chkRememberMe`, `chkAgree` |
| **RadioButton** | `rdo` / `rad`     | `rdoMale`, `radFemale`      |
| **ComboBox**    | `cmb` / `cbo`     | `cmbCity`, `cmbRole`        |
| **ListBox**     | `lst`             | `lstProducts`, `lstLogs`    |
| **PictureBox**  | `pic` / `pbx`     | `picAvatar`, `picLogo`      |
| **ProgressBar** | `prg` / `pbr`     | `prgDownload`, `pbrUpload`  |
| **RichTextBox** | `rtf` / `rtx`     | `rtfContent`, `rtxEditor`   |

2. 容器控件 (Containers)

| 控件类型 (Type)    | 推荐前缀 (Prefix) | 命名示例 (Example)           |
| ------------------ | ----------------- | ---------------------------- |
| **Panel**          | `pnl`             | `pnlHeader`, `pnlContainer`  |
| **GroupBox**       | `grp` / `gbx`     | `grpUserInfo`, `gbxSettings` |
| **TabControl**     | `tab`             | `tabMain`, `tabSettings`     |
| **TabPage**        | `tbp` / `pag`     | `tbpGeneral`, `pagAdvanced`  |
| **SplitContainer** | `spl` / `spc`     | `splMain`, `spcLayout`       |

3. 数据与视图控件 (Data & Views)

| 控件类型 (Type)   | 推荐前缀 (Prefix) | 命名示例 (Example)        |
| ----------------- | ----------------- | ------------------------- |
| **DataGridView**  | `dgv`             | `dgvOrders`, `dgvUsers`   |
| **TreeView**      | `tvw` / `tv`      | `tvwMenu`, `tvCategories` |
| **ListView**      | `lvw` / `lv`      | `lvwFiles`, `lvItems`     |
| **BindingSource** | `bds` / `bs`      | `bdsOrders`, `bsCustomer` |

4. 菜单与工具栏 (Menus & Toolbars)

| 控件类型 (Type)       | 推荐前缀 (Prefix) | 命名示例 (Example)             |
| --------------------- | ----------------- | ------------------------------ |
| **MenuStrip**         | `mns` / `ms`      | `mnsMain`, `msMainMenu`        |
| **ToolStripMenuItem** | `tsmi`            | `tsmiFileOpen`, `tsmiEditCopy` |
| **ContextMenuStrip**  | `cms`             | `cmsGridRightClick`            |
| **ToolStrip**         | `tls` / `ts`      | `tlsActions`, `tsShortcuts`    |
| **ToolStripButton**   | `tsbtn`           | `tsbtnSave`, `tsbtnDelete`     |
| **StatusStrip**       | `sts` / `ss`      | `stsFooter`, `ssStatus`        |

5. 组件与对话框 (Components & Dialogs)

| 控件类型 (Type)         | 推荐前缀 (Prefix) | 命名示例 (Example)       |
| ----------------------- | ----------------- | ------------------------ |
| **Timer**               | `tmr`             | `tmrRefresh`, `tmrClock` |
| **OpenFileDialog**      | `ofd`             | `ofdSelectImage`         |
| **SaveFileDialog**      | `sfd`             | `sfdExportExcel`         |
| **FolderBrowserDialog** | `fbd`             | `fbdSelectPath`          |